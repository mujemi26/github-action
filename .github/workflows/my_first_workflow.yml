name: My First Workflow
on: [push]

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:          
            - name: Print a message
              run: echo "Hello, world!"
            - name: List Files
              run: ls
            - name: Read Files
              run: cat README.md